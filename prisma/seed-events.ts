// Backward-compatible entry point. The comprehensive seeder now covers every
// event template plus matches, tables, packages, members, transactions, CMS,
// gallery videos, and media records.
import "./seed";
